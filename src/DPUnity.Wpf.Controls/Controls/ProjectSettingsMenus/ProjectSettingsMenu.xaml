<UserControl x:Class="DPUnity.Wpf.Controls.Controls.ProjectSettingsMenus.ProjectSettingsMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DPUnity.Wpf.Controls.Controls.ProjectSettingsMenus"
             xmlns:icons="https://dpunity.com/dev/wpf/ui/controls/packicon"
             xmlns:converters="clr-namespace:DPUnity.Wpf.Controls.Converters"
             xmlns:itemIcon="clr-namespace:DPUnity.Wpf.UI.Behaviors;assembly=DPUnity.Wpf.UI"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Width="210"
             x:Name="Root">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:BooleanToHiddenVisibilityConverter x:Key="BooleanToHiddenVisibilityConverter" />
            <local:StringToHasValueConverter x:Key="StringToHasValueConverter" />
            <SolidColorBrush x:Key="DarkPrimaryBrush"
                             Color="#03AB54" />
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <GroupBox Header="Dự án hiện tại"
                  DockPanel.Dock="Top">
            <TextBlock Text="{Binding CurrentProjectName, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource DarkDangerBrush}"
                       FontWeight="Bold"
                       FontSize="18" />
        </GroupBox>
        <GroupBox Header="Danh sách thiết lập">
            <DockPanel LastChildFill="True">
                <Grid DockPanel.Dock="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Style="{DynamicResource DP_IconButtonStyle}"
                            Content="Plus"
                            ToolTip="Tạo mới"
                            Tag="Plus"
                            Margin="3"
                            HorizontalAlignment="Left"
                            Command="{Binding AddNewCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
                    <Button Style="{DynamicResource DP_IconButtonStyle}"
                            ToolTip="Khôi phục"
                            Grid.Column="1"
                            Content="DatabaseCheckOutline"
                            Tag="Plus"
                            Margin="3"
                            HorizontalAlignment="Left"
                            Command="{Binding AddNewCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
                    <hc:TextBox Margin="3"
                                Grid.Column="2"
                                MinWidth="127"
                                hc:InfoElement.ShowClearButton="True"
                                hc:InfoElement.Placeholder="Tìm kiếm"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Style="{DynamicResource TextBoxPlusBaseStyle}"
                                Height="28"
                                Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, UpdateSourceTrigger=PropertyChanged}"
                                TextChanged="Search_TextChanged" />
                </Grid>
                <Grid Margin="0">
                    <ListView x:Name="ProjectSettingList"
                              Style="{DynamicResource ListViewWithoutBorder}"
                              ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                              SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                              Visibility="{Binding HasItems, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Converter={StaticResource BooleanToHiddenVisibilityConverter}}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Vertical">
                                        <icons:PackIcon x:Name="activeIcon"
                                                        Kind="Ticket"
                                                        Height="24"
                                                        Width="14"
                                                        Foreground="{DynamicResource PrimaryBrush}"
                                                        Visibility="{Binding IsActivated, Converter={StaticResource BooleanToHiddenVisibilityConverter}}" />
                                        <Button Background="Transparent"
                                                Cursor="Hand"
                                                Margin="0 -2 0 0"
                                                Command="{Binding SaveCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource PrimaryTextBrush}" />
                                                    <Setter Property="Padding"
                                                            Value="5" />
                                                    <Setter Property="Visibility"
                                                            Value="Collapsed" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="Transparent">
                                                                    <icons:PackIcon x:Name="icon"
                                                                                    Kind="ContentSave"
                                                                                    Height="14"
                                                                                    Foreground="{TemplateBinding Foreground}"
                                                                                    Width="14" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver"
                                                                             Value="True">
                                                                        <Setter Property="Foreground"
                                                                                Value="{DynamicResource PrimaryBrush}" />
                                                                        <Setter TargetName="icon"
                                                                                Property="Kind"
                                                                                Value="ContentSave" />
                                                                    </Trigger>
                                                                    <Trigger Property="IsPressed"
                                                                             Value="True">
                                                                        <Setter Property="Foreground"
                                                                                Value="{DynamicResource PrimaryBrush}" />
                                                                        <Setter TargetName="icon"
                                                                                Property="Kind"
                                                                                Value="ContentSave" />
                                                                    </Trigger>
                                                                    <Trigger Property="Visibility"
                                                                             Value="Hidden">
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}}"
                                                                     Value="True">
                                                            <Setter Property="Visibility"
                                                                    Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                Orientation="Vertical"
                                                Margin="5">
                                        <TextBlock Text="{Binding Name}"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource PrimaryTextBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}}"
                                                                     Value="True">
                                                            <Setter Property="FontWeight"
                                                                    Value="Bold" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}}"
                                                                     Value="True">
                                                            <Setter Property="FontWeight"
                                                                    Value="Bold" />
                                                            <Setter Property="Foreground"
                                                                    Value="{DynamicResource PrimaryBrush}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Margin="0 4 0 0">
                                            <InlineUIContainer>
                                                <icons:PackIcon Kind="Update"
                                                                Height="14"
                                                                Width="14"
                                                                Foreground="{DynamicResource SecondaryTextBrush}" />
                                            </InlineUIContainer>
                                            <Run Text="{Binding UpdatedAt, StringFormat=': {0:dd/MM/yyyy HH:mm:ss}', TargetNullValue='None'}"
                                                 FontSize="12"
                                                 Foreground="{DynamicResource SecondaryTextBrush}"
                                                 BaselineAlignment="Center" />
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <Button x:Name="MenuButton"
                                                Background="Transparent"
                                                Click="MenuButton_Click"
                                                Grid.Column="2"
                                                Margin="0 2 10 0"
                                                Cursor="Hand">
                                            <Button.ContextMenu>
                                                <ContextMenu Style="{DynamicResource DP_ContextMenuStyle}">
                                                    <MenuItem Header="Kích hoạt"
                                                              itemIcon:MenuItemIconBehavior.IconKind="Ticket"
                                                              Click="ActivateMenuItem_Click" />
                                                    <MenuItem Header="Sửa"
                                                              itemIcon:MenuItemIconBehavior.IconKind="Edit"
                                                              Click="EditMenuItem_Click" />
                                                    <MenuItem Header="Copy"
                                                              itemIcon:MenuItemIconBehavior.IconKind="ContentCopy"
                                                              Click="CopyMenuItem_Click" />
                                                    <MenuItem Header="Xóa"
                                                              itemIcon:MenuItemIconBehavior.IconKind="Delete"
                                                              Click="DeleteMenuItem_Click" />
                                                    <MenuItem Header="Chọn phiên bản"
                                                              itemIcon:MenuItemIconBehavior.IconKind="Artstation"
                                                              Click="DeleteMenuItem_Click" />
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Grid Background="Transparent"
                                                                      Height="20">
                                                                    <icons:PackIcon Kind="DotsVertical"
                                                                                    Height="14"
                                                                                    Width="14" />
                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource PrimaryTextBrush}" />
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <!-- Thông báo khi không tìm thấy kết quả -->
                    <StackPanel Orientation="Vertical"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="20">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasItems, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                 Value="False">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <icons:PackIcon Kind="DatabaseSearchOutline"
                                        Height="48"
                                        Width="48"
                                        Foreground="{DynamicResource SecondaryTextBrush}"
                                        HorizontalAlignment="Center" />
                        <TextBlock Text="Không tìm thấy dự án nào"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   FontSize="14"
                                   HorizontalAlignment="Center"
                                   Margin="0 10 0 0" />
                        <TextBlock Foreground="{DynamicResource ThirdlyTextBrush}"
                                   FontSize="10"
                                   HorizontalAlignment="Center"
                                   Margin="0 5 0 0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility"
                                            Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SearchText, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Converter={StaticResource StringToHasValueConverter}}"
                                                     Value="True">
                                            <Setter Property="Visibility"
                                                    Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="Thử tìm kiếm với từ khóa khác" />
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </DockPanel>
        </GroupBox>
    </DockPanel>
</UserControl>