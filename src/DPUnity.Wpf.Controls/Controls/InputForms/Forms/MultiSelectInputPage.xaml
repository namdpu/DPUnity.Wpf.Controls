<wd:DPage x:Class="DPUnity.Wpf.Controls.Controls.InputForms.Forms.MultiSelectInputPage"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:local="clr-namespace:DPUnity.Wpf.Controls.Controls.InputForms.Forms"
          xmlns:hc="https://handyorg.github.io/handycontrol"
          xmlns:wd="https://dpunity.com/dev/wpf/windows"
          xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
          mc:Ignorable="d"
          d:DesignHeight="450"
          d:DesignWidth="800"
          Title="MultiSelectInputPage">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <hc:SearchBar Margin="0 10 5 10"
                          hc:InfoElement.Placeholder="Tìm kiếm"
                          hc:InfoElement.ShowClearButton="True"
                          Style="{DynamicResource SearchBarExtendBaseStyle}"
                          Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Sort"
                    Grid.Column="1"
                    Style="{DynamicResource DP_IconButtonStyle}" 
                    Command="{Binding LeftSortCommand}"/>
        </Grid>
        <Grid Grid.Row="1"
              Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <hc:SearchBar Margin="0 10 5 10"
                          hc:InfoElement.Placeholder="Tìm kiếm"
                          hc:InfoElement.ShowClearButton="True"
                          Style="{DynamicResource SearchBarExtendBaseStyle}"
                          Text="{Binding SelectedSearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Sort"
                    Grid.Column="1"
                    Style="{DynamicResource DP_IconButtonStyle}" 
                    Command="{Binding RightSortCommand}"/>
        </Grid>

        <Grid Grid.Row="2">
            <ListView x:Name="LeftListView"
                      ItemsSource="{Binding ItemsSource}"
                      SelectionChanged="LeftListView_SelectionChanged"
                      Style="{DynamicResource DP_ListViewStyle.Small}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch" />
                        <Setter Property="Height"
                                Value="28" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border x:Name="Bd"
                                            SnapsToDevicePixels="true"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0 0.5 0 0.5"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}"
                                                          Content="{TemplateBinding Content}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver"
                                                 Value="true">
                                            <Setter Property="Background"
                                                    TargetName="Bd"
                                                    Value="{DynamicResource ThirdlyRegionBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected"
                                                 Value="true">
                                            <Setter Property="Background"
                                                    TargetName="Bd"
                                                    Value="{DynamicResource RegionBrush}" />
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource PrimaryBrush}" />
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected"
                                                           Value="true" />
                                                <Condition Property="Selector.IsSelectionActive"
                                                           Value="false" />
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background"
                                                    TargetName="Bd"
                                                    Value="{DynamicResource RegionBrush}" />
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding MoveSingleRightCommand}"
                                               CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListView}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"
                                   Padding="5" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock Text="No data"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="Gray"
                       FontSize="14">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility"
                                Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ItemsSource.Count}"
                                         Value="0">
                                <Setter Property="Visibility"
                                        Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
        
        <hc:UniformSpacingPanel Grid.Row="2"
                                Spacing="3"
                                Orientation="Vertical"
                                Grid.Column="1"
                                Margin="5 0"
                                VerticalAlignment="Center">
            <Button Style="{DynamicResource DP_IconButtonHighLightStyle}"
                    Content="ArrowRightThin"
                    Command="{Binding MoveRightCommand}" />
            <Button Style="{DynamicResource DP_IconButtonHighLightStyle}"
                    Content="ArrowExpandRight"
                    Margin="0 0 0 7"
                    Command="{Binding MoveRightAllCommand}" />
            <Button Style="{DynamicResource DP_IconButtonHighLightStyle}"
                    Content="ArrowLeftThin"
                    Margin="0 7 0 0"
                    Command="{Binding MoveLeftCommand}" />
            <Button Style="{DynamicResource DP_IconButtonHighLightStyle}"
                    Content="ArrowExpandLeft"
                    Command="{Binding MoveLeftAllCommand}" />
        </hc:UniformSpacingPanel>
        
        <Grid Grid.Row="2"
              Grid.Column="2">
            <ListView x:Name="RightListView"
                      ItemsSource="{Binding FilteredSelectedItems}"
                      SelectionChanged="RightListView_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment"
                                Value="Stretch" />
                        <Setter Property="Height"
                                Value="28" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border x:Name="Bd"
                                            SnapsToDevicePixels="true"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="0 0.5 0 0.5"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}"
                                                          Content="{TemplateBinding Content}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver"
                                                 Value="true">
                                            <Setter Property="Background"
                                                    TargetName="Bd"
                                                    Value="{DynamicResource ThirdlyRegionBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected"
                                                 Value="true">
                                            <Setter Property="Background"
                                                    TargetName="Bd"
                                                    Value="{DynamicResource RegionBrush}" />
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource PrimaryBrush}" />
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected"
                                                           Value="true" />
                                                <Condition Property="Selector.IsSelectionActive"
                                                           Value="false" />
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background"
                                                    TargetName="Bd"
                                                    Value="{DynamicResource RegionBrush}" />
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsChecked}"
                                         Value="True">
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding MoveSingleLeftCommand}"
                                               CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ListView}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock Text="No data"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource SecondaryTextBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility"
                                Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding FilteredSelectedItems.Count}"
                                         Value="0">
                                <Setter Property="Visibility"
                                        Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
        
        <hc:UniformSpacingPanel Spacing="5"
                                Grid.Row="3"
                                Grid.ColumnSpan="3"
                                HorizontalAlignment="Right"
                                Margin="0 10 0 0">
            <Button Content="OK"
                    Width="75"
                    IsDefault="True"
                    Command="{Binding SubmitCommand}" />
            <Button Content="Cancel"
                    Width="75"
                    Style="{DynamicResource ButtonDanger}"
                    Command="{Binding CancelCommand}"
                    HorizontalAlignment="Left" />
        </hc:UniformSpacingPanel>
    </Grid>
</wd:DPage>