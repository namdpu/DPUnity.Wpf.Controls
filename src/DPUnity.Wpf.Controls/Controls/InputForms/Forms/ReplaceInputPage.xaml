<wd:DPage x:Class="DPUnity.Wpf.Controls.Controls.InputForms.Forms.ReplaceInputPage"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:local="clr-namespace:DPUnity.Wpf.Controls.Controls.InputForms.Forms"
          xmlns:wd="https://dpunity.com/dev/wpf/windows"
          xmlns:hc="https://handyorg.github.io/handycontrol"
          mc:Ignorable="d"
          d:DesignHeight="500"
          d:DesignWidth="400">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Text="Replace:"
                   Margin="0 0 0 5" />
        
        <hc:TextBox Text="{Binding Replace, UpdateSourceTrigger=PropertyChanged}"
                    Style="{DynamicResource TextBoxExtend}"
                    Height="28"
                    Grid.Row="1" />
        
        
        <TextBlock Text="Replace with:"
                   Margin="0 10 0 5"
                   Grid.Row="2" />
        <hc:TextBox Text="{Binding ReplaceWith, UpdateSourceTrigger=PropertyChanged}"
                    Style="{DynamicResource TextBoxExtend}"
                    Height="28"
                    Grid.Row="3" />
        
        
        <hc:UniformSpacingPanel Spacing="10"
                                Orientation="Horizontal"
                                Grid.Row="4"
                                HorizontalAlignment="Right"
                                Margin="0 10">
            <Button Content="OK"
                    Width="75"
                    IsDefault="True"
                    Command="{Binding SubmitCommand}" />
            <Button Content="Cancel"
                    Width="75"
                    IsCancel="True"
                    Style="{DynamicResource ButtonDanger}"
                    Command="{Binding CancelCommand}" />
        </hc:UniformSpacingPanel>
    </Grid>
</wd:DPage>